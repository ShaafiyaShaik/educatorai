name: Vercel Deploy

on:
  workflow_dispatch: {}
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies (frontend)
        working-directory: educator-ai-assistant/frontend
        run: |
          npm ci

      - name: Build frontend
        working-directory: educator-ai-assistant/frontend
        run: |
          npm run build

      - name: Check VERCEL_TOKEN secret
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          if [ -z "$VERCEL_TOKEN" ]; then
            echo "ERROR: VERCEL_TOKEN secret is not set. Add it in GitHub repo Settings → Secrets and variables → Actions -> New repository secret (name: VERCEL_TOKEN)." >&2
            exit 1
          else
            echo "VERCEL_TOKEN is present (hidden). Proceeding to deploy step."
          fi

      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        working-directory: educator-ai-assistant/frontend
        run: |
          # Install vercel CLI and deploy the built app
          npm install -g vercel
          # `--confirm` avoids interactive prompts; `--prod` creates a production deployment
          npx vercel --prod --token "$VERCEL_TOKEN" --confirm

      - name: Output deployment url
        if: success()
        run: echo "Deployment finished. Check Vercel dashboard for details."
